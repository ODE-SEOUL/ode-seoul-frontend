// import { getAsync } from "./common";
// import { GetUserListDto } from "../types/userList";

// export const getUserList = async (id) => {
//   const response = await getAsync<GetUserListDto, undefined>(`/user/${id}`);
//   return response;
// };
